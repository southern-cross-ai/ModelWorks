```
git pull origin deleteme_quickfix
```
```
chmod +x init.sh
./init.sh
```
(delete ENTRYPOINT line in backend's Dockerfile)
Start the backend container again when you're ready
OR make whatever changes you need, run `./init.sh` again
